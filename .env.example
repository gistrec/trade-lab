# Local data directory for cached candles (Parquet files).
TRADE_LAB_DATA_DIR=data

# Default ccxt exchange id (e.g. binance, bybit, kraken).
TRADE_LAB_EXCHANGE=binance

# Backtest defaults.
TRADE_LAB_INITIAL_CAPITAL=10000
TRADE_LAB_FEE_RATE=0.001
TRADE_LAB_SLIPPAGE_RATE=0.0005
